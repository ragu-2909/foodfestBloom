Drop your company logo here as "company-logo.png" (or edit EventLogo.tsx to
point at a .svg instead). It will appear automatically in the header on every
page — Vote, Display, Admin, and the color-selection flow — once the file
exists. No code changes needed. Until then, a chef-hat placeholder is shown.
